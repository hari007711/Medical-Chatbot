from setuptools import find_packages, setup

setup(
    name='Generative AI Project',  # Package name should be lowercase and underscore-separated
    version='0.0.0',               # Use a valid semantic version
    author='Bappy ahmed',
    author_email='entbappy73@gmail.com',
    packages=find_packages(),             # Maps the root package to 'src'
    install_requires=[],                  # Add dependencies here as a list
    
)
